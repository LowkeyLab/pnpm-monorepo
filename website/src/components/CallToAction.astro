---
interface Props {
  heading?: string;
  className?: string;
}

const { heading = "Interested in working together?", className = "mt-16" } =
  Astro.props;
---

<div class={`text-center ${className}`}>
  <h2 class="text-2xl font-bold mb-4">{heading}</h2>
  <div class="flex justify-center gap-4">
    <a
      href="mailto:timdttran2000@gmail.com"
      target="_blank"
      rel="noreferrer"
      class="btn btn-primary"
    >
      Get in Touch
    </a>
  </div>
</div>
